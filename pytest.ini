[pytest]
testpaths = tests
python_files = test_*.py
markers =
    face: face-identification and people-grouping tests
    photo_library: tests that use a real local photo library
    slow: tests that may take longer because they scan real media files
